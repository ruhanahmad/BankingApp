import 'package:flutter/material.dart';

class MetalPlan extends StatelessWidget {
  const MetalPlan({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 300,
      color: Colors.blue,
    );
  }
}
